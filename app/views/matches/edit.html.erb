<h2 class="my-5">End your buddy match</h2>

<%# FORM HERE %>

<div class="d-flex flex-column align-items-center">
  <div class="mt-4 listing-form">

      <%= simple_form_for @match do |f| %>
        <%= f.input :status, as: :select, collection: Match::STATUSES, :selected => "cancelled", html_input: "required" %>
        <%= f.submit label: "End buddy relationship" class: "btn btn-primary my-4"%>
        <%# NOTE: CHECK HOW TO EDIT BUTTON TEXT %>
    <% end %>

  </div>
</div>
